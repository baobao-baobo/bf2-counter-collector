# bf2-counter-collector

Config-driven performance counter collector for the NVIDIA BlueField-2 DPU.

> Work in progress — full documentation is being assembled; the collector
> source and configuration files are under `code/` and `configs/`.
